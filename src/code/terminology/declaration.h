// a declaration gives the compiler information about the signature of a function.
// that way, it will know the return type, the name and the parameters of the
// function.
// however it does not matter to the compiler what the implementation looks like.
int this_is_a_declaration(int a, char b);